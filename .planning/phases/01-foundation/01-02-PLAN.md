---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/Button.astro
  - src/components/Card.astro
  - src/components/SectionWrapper.astro
  - src/components/FormInput.astro
  - src/layouts/Base.astro
autonomous: true

must_haves:
  truths:
    - "Desktop navigation shows logo, page links (Home, About, Courses), and a Get Started CTA button"
    - "On mobile (<768px), navigation collapses to a hamburger icon that opens a full-width menu"
    - "Pressing Escape closes the mobile menu"
    - "Header stays fixed at the top of the viewport when scrolling"
    - "Button component renders in three visual variants (gold primary, dark secondary, transparent ghost)"
    - "Card component shows a dark container with border that is visually distinct from the page background"
    - "Form input has a dark background with visible border and gold focus ring"
    - "Footer displays navigation links and copyright text"
  artifacts:
    - path: "src/components/Header.astro"
      provides: "Fixed header with desktop nav and mobile hamburger menu"
      min_lines: 60
    - path: "src/components/Footer.astro"
      provides: "Site footer with nav links and copyright"
      min_lines: 20
    - path: "src/components/Button.astro"
      provides: "Reusable button with primary/secondary/ghost variants and sm/md/lg sizes"
      exports: ["default"]
    - path: "src/components/Card.astro"
      provides: "Dark themed card container"
      exports: ["default"]
    - path: "src/components/SectionWrapper.astro"
      provides: "Consistent section padding with narrow/wide width options"
      exports: ["default"]
    - path: "src/components/FormInput.astro"
      provides: "Dark themed form input with focus ring"
      exports: ["default"]
    - path: "src/layouts/Base.astro"
      provides: "Updated layout with Header and Footer"
      contains: "Header"
  key_links:
    - from: "src/layouts/Base.astro"
      to: "src/components/Header.astro"
      via: "import and render with currentPath prop"
      pattern: "Header.*currentPath"
    - from: "src/layouts/Base.astro"
      to: "src/components/Footer.astro"
      via: "import and render"
      pattern: "import Footer"
    - from: "src/components/Header.astro"
      to: "mobile-menu script"
      via: "inline script tag for toggle behavior"
      pattern: "mobile-menu-toggle"
---

<objective>
Build the complete shared component library (Header with responsive navigation, Footer, Button, Card, SectionWrapper, FormInput) and wire Header + Footer into the Base layout. After this plan, every page automatically gets navigation and footer.

Purpose: These components are used on every page. Building them as a library before page shells ensures consistency and prevents duplication.
Output: Six Astro components, updated Base layout with Header/Footer, working desktop and mobile navigation.
</objective>

<execution_context>
@C:\Users\kelvi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\kelvi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\roseyco\Bwebsite\.planning\PROJECT.md
@D:\roseyco\Bwebsite\.planning\ROADMAP.md
@D:\roseyco\Bwebsite\.planning\phases\01-foundation\01-RESEARCH.md
@D:\roseyco\Bwebsite\.planning\phases\01-foundation\01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Header with responsive navigation and Footer</name>
  <files>
    src/components/Header.astro
    src/components/Footer.astro
    src/layouts/Base.astro
  </files>
  <action>
    1. Create src/components/Header.astro -- the most complex component in this plan.

       Requirements:
       - Fixed position at top of viewport (`fixed top-0 left-0 right-0 z-50`)
       - Semi-transparent dark background with backdrop blur (`bg-navy-900/95 backdrop-blur-sm`)
       - Bottom border for visual separation (`border-b border-navy-700/50`)
       - Height: h-18 (4.5rem, matches --header-height CSS variable)

       Desktop nav (visible at md: and above):
       - Logo text "Elevateo" on the left (white, bold, tracking-tight)
       - Three nav links: Home (/), About (/about/), Courses (/courses/)
       - Links styled as uppercase, small text, tracking-wide
       - Active page link highlighted in accent color (text-accent)
       - Inactive links in text-secondary, hover to white
       - "Get Started" CTA button on the right (gold accent, rounded)

       Mobile nav (visible below md:):
       - Hamburger icon button replaces nav links
       - Click toggles a full-width dropdown panel below the header
       - Panel contains same nav links as vertical list + Get Started CTA
       - Close icon (X) replaces hamburger when menu is open
       - Icons swap visibility via toggling `hidden` class

       Accept a `currentPath` prop (string) to determine which nav link is active. Use `class:list` for conditional active styling.

       Include a `<script>` tag (NOT inside frontmatter) for mobile menu behavior:
       - Toggle menu visibility on button click
       - Swap hamburger/close icons
       - Set `aria-expanded` attribute on button
       - Set `document.body.style.overflow = "hidden"` when menu open, `""` when closed
       - Close on Escape key press
       - Close when clicking any link in the mobile menu
       - NOTE: Astro scripts are type="module" by default -- no DOMContentLoaded needed

       Include the header spacer div AFTER the header: `<div class="h-18" aria-hidden="true"></div>`

       Accessibility:
       - Button has `aria-label="Toggle navigation menu"` and `aria-expanded="false"`
       - Desktop nav has `aria-label="Main navigation"`
       - Mobile nav has `aria-label="Mobile navigation"`

       Use the EXACT component code pattern from the RESEARCH.md section 4 (Complete Header Component Pattern) as the base, with these nav links:
       ```javascript
       const navLinks = [
         { label: "Home", href: "/" },
         { label: "About", href: "/about/" },
         { label: "Courses", href: "/courses/" },
       ];
       ```

    2. Create src/components/Footer.astro:

       - Dark background (`bg-navy-950`) with top border (`border-t border-navy-700/50`)
       - Container-wide layout with padding
       - Three sections in a responsive grid:
         a. Brand section: "Elevateo Co" name + one-line tagline ("Helping business owners scale.")
         b. Navigation section: Same links as header (Home, About, Courses) as a vertical list
         c. Social placeholder section: Text like "Follow us" with placeholder icons or text links (Twitter, LinkedIn, YouTube) -- these will be real links later
       - Bottom bar: Copyright line "2026 Elevateo Co. All rights reserved." in tertiary text
       - Links styled as text-secondary, hover to white

    3. Update src/layouts/Base.astro to import and render Header and Footer:

       Add to the frontmatter imports:
       ```
       import Header from "../components/Header.astro";
       import Footer from "../components/Footer.astro";
       ```

       Update the body to include Header before main and Footer after main:
       ```html
       <body class="min-h-screen flex flex-col">
         <Header currentPath={Astro.url.pathname} />
         <main class="flex-1">
           <slot />
         </main>
         <Footer />
       </body>
       ```

       CRITICAL: Pass `Astro.url.pathname` as the `currentPath` prop to Header so active link highlighting works.
  </action>
  <verify>
    Run `pnpm dev` and verify:
    1. Header is fixed at top with logo and nav links visible on desktop
    2. Active page link (Home on index) shows in gold accent color
    3. Resize browser below 768px -- hamburger icon appears, desktop links hide
    4. Click hamburger -- mobile menu slides open with all links
    5. Click hamburger again or press Escape -- menu closes
    6. Footer appears at bottom with brand, nav links, and copyright
    7. Body does NOT scroll when mobile menu is open
    Run `pnpm build` to confirm no build errors.
  </verify>
  <done>
    Header shows fixed navigation with desktop links and working mobile hamburger menu toggle. Footer displays at page bottom. Both are wired into Base.astro and appear on every page.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Button, Card, SectionWrapper, and FormInput components</name>
  <files>
    src/components/Button.astro
    src/components/Card.astro
    src/components/SectionWrapper.astro
    src/components/FormInput.astro
    src/pages/index.astro (update test page to showcase all components)
  </files>
  <action>
    1. Create src/components/Button.astro:

       Props interface:
       - `variant`: "primary" | "secondary" | "ghost" (default: "primary")
       - `size`: "sm" | "md" | "lg" (default: "md")
       - `href`: optional string (if provided, renders as `<a>`, otherwise `<button>`)
       - `class`: optional string for additional classes

       Variant styles:
       - primary: `bg-accent hover:bg-accent-hover text-navy-900` (gold button, dark text)
       - secondary: `bg-navy-700 hover:bg-navy-600 text-white border border-navy-600`
       - ghost: `bg-transparent hover:bg-navy-800 text-text-secondary hover:text-white`

       Size styles:
       - sm: `text-sm px-4 py-2`
       - md: `text-sm px-6 py-3`
       - lg: `text-body px-8 py-4`

       Base classes (all variants): `inline-flex items-center justify-center font-bold rounded-lg transition-colors duration-200`

       Render as `<a>` when href is provided, `<button>` otherwise. Use a dynamic Tag variable.
       Accept children via `<slot />`.

       Follow the EXACT pattern from RESEARCH.md section 5 (Component Props Pattern).

    2. Create src/components/Card.astro:

       Props: `class` (optional string)
       Styles: `bg-navy-800 border border-navy-700/50 rounded-xl p-6 sm:p-8 transition-colors hover:border-navy-600`
       Content via `<slot />`
       Use `class:list` to merge custom classes.

    3. Create src/components/SectionWrapper.astro:

       Props:
       - `id`: optional string
       - `width`: "narrow" | "wide" (default: "wide")
       - `class`: optional string

       Width map:
       - narrow: `max-w-3xl`
       - wide: `max-w-7xl`

       Structure: `<section>` with `.section` class (from global.css) wrapping a `<div>` with width class + `mx-auto px-4 sm:px-6 lg:px-8`.
       Content via `<slot />`.

    4. Create src/components/FormInput.astro:

       Props: `type` (default "text"), `name` (required), `placeholder`, `required` (default false), `class`
       Styles: `w-full bg-navy-800 border border-navy-600 text-white placeholder-text-tertiary rounded-lg px-4 py-3 text-body focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent transition-colors duration-200`

    5. Update src/pages/index.astro to showcase ALL components:

       Replace the previous design token test page with a comprehensive component showcase that imports and renders:
       - SectionWrapper (both narrow and wide)
       - Button (all 3 variants x 3 sizes = show at least 1 of each variant and 1 of each size)
       - Card (2-3 cards in a grid)
       - FormInput (email input with placeholder "Enter your email")
       - The design token colors/typography section from before can remain as a subsection

       This test page serves as the component library reference until real page content is built in Plan 01-03.
  </action>
  <verify>
    Run `pnpm dev` and verify on the test page:
    1. Button primary: gold background with dark text
    2. Button secondary: dark background with light border
    3. Button ghost: transparent, text only, hover shows dark background
    4. Button sizes visibly differ (sm < md < lg)
    5. Cards have dark background distinct from page background, visible border
    6. SectionWrapper constrains content width (narrow visibly narrower than wide)
    7. FormInput has dark background, visible border, and gold focus ring when clicked
    8. All components render within the Header/Footer layout
    Run `pnpm build` to confirm no build errors.
  </verify>
  <done>
    All six shared components (Header, Footer, Button, Card, SectionWrapper, FormInput) are built, render correctly on the test page, and are ready for use in page shells. Button has 3 variants and 3 sizes. Navigation works on desktop and mobile.
  </done>
</task>

</tasks>

<verification>
1. Header is fixed, has desktop nav + mobile hamburger, and active link highlighting works
2. Mobile menu opens/closes with toggle, Escape key, and link clicks
3. Body scroll is locked when mobile menu is open
4. Footer has brand, navigation, and copyright sections
5. Button renders 3 distinct variants (primary/secondary/ghost) and 3 sizes (sm/md/lg)
6. Card visually separates from page background with border
7. SectionWrapper constrains content at two widths (narrow/wide)
8. FormInput has gold focus ring when focused
9. All components are accessible (aria attributes on Header, semantic HTML)
10. `pnpm build` succeeds
</verification>

<success_criteria>
- Six Astro components exist and render correctly
- Header + Footer are wired into Base.astro and appear on every page
- Desktop navigation shows all 3 page links + CTA
- Mobile hamburger menu opens, closes, and locks body scroll
- Active page link is highlighted in gold accent
- Component test page showcases all components
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
