---
phase: 07-lachlan-section-integration-navigation-anchors
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/pages/index.astro
autonomous: false
requirements:
  - MENTOR-01
  - MENTOR-02
  - MENTOR-03
  - MENTOR-04
  - MENTOR-05
  - MENTOR-06
  - MENTOR-07
must_haves:
  truths:
    - "Visitor scrolling past Allan's About section sees a 1px hair-line divider, then lands in Lachlan's bio split-grid"
    - "Lachlan's photo renders on the LEFT and his bio text renders on the RIGHT at lg+ breakpoints (flipped from Allan's layout)"
    - "Lachlan's bio names Bellum Advisors, boltloop.co, and solo agency scaling"
    - "Lachlan's bio includes a one-line discipline / training reference framed as work-ethic metaphor (no gym-bro words)"
    - "Visitor sees three specialty cards titled 'Solo Agency Scaling', 'Co-Founder Operator', 'AI & Automation'"
    - "Visitor sees a 'Who Lachlan works with' block with 3 audience items (Solo agency owners / Early-stage founders / Ops-heavy businesses)"
    - "Visitor clicks Lachlan's CTA and is taken to Allan's Calendly link in a new tab (documented placeholder)"
    - "Clicking 'Lachlan' in the header (delivered by Plan 01) smooth-scrolls to Lachlan's section eyebrow visible below the fixed header"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "MentorIntro invocation + 3 Lachlan content data blocks + hair-line divider"
      contains: "<MentorIntro"
    - path: "src/pages/index.astro"
      provides: "MentorIntro import in frontmatter"
      contains: 'import MentorIntro from "../components/MentorIntro.astro"'
  key_links:
    - from: "src/pages/index.astro lachlanCredentials/Specialties/Audience consts"
      to: "<MentorIntro> invocation props"
      via: "props pass-through"
      pattern: "credentials=\\{lachlanCredentials\\}"
    - from: "Header anchor #lachlan (Plan 01)"
      to: "MentorIntro outer <section id={id}>"
      via: 'id="lachlan" prop'
      pattern: 'id="lachlan"'
    - from: "MentorIntro `cta.href` prop"
      to: "Allan's Calendly URL"
      via: "documented placeholder"
      pattern: "calendly\\.com/allan-chan-roseyco/one-on-one"
    - from: "Allan's About </section> close (line 153)"
      to: "<MentorIntro /> open"
      via: "1px hair-line divider <div class=\"border-t border-white/5\"></div>"
      pattern: "border-t border-white/5"
---

<objective>
Insert Lachlan MacDonald's full mentor section into the single-page site between Allan's About section and the Companies section. This plan satisfies MENTOR-01 through MENTOR-07 in one focused edit to `src/pages/index.astro`.

Purpose: Phase 6 already shipped the `MentorIntro.astro` component (props-driven shell with bio split-grid + specialty cards + audience list) and the optimized `lachlan-portrait.webp` (164 KB on disk). Phase 7 Plan 02 is the wiring step — it adds the import, drafts three locked-content data blocks (credentials, specialties, audience), inserts a hair-line divider after Allan's section, and invokes `<MentorIntro>` with `id="lachlan"`, `flip={true}`, and the placeholder Calendly CTA. Zero new components, zero new assets.

Output:
- `src/pages/index.astro` — frontmatter import added; three `const` data blocks (`lachlanCredentials`, `lachlanSpecialties`, `lachlanAudience`); a hair-line `<div class="border-t border-white/5"></div>` between line 153 and line 155; a single `<MentorIntro .../>` invocation with all props per UI-SPEC §Copywriting Contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-lachlan-section-integration-navigation-anchors/07-RESEARCH.md
@.planning/phases/07-lachlan-section-integration-navigation-anchors/07-UI-SPEC.md
@.planning/phases/07-lachlan-section-integration-navigation-anchors/07-VALIDATION.md
@.planning/phases/07-lachlan-section-integration-navigation-anchors/07-01-PLAN.md
@src/components/MentorIntro.astro

<interfaces>
<!-- MentorIntro.astro Props interface (delivered Phase 6 — DO NOT MODIFY the component, only invoke it) -->

```typescript
interface Specialty {
  icon: string;                 // Filename in public/images/icons/ WITHOUT extension
                                // Component expands to `/images/icons/${icon}.webp`
  name: string;                 // Card title (2-5 words)
  desc: string;                 // Card body (1-2 sentences, <= 180 chars)
}

interface AudienceItem {
  title: string;                // Bold lead phrase (2-4 words), rendered as <strong>
  desc: string;                 // Trailing description after em-dash
}

interface Props {
  id?: string;                  // HTML id on outer bio section — pass "lachlan" for #lachlan anchor
  eyebrow: string;              // Small gold label above H2 (rendered uppercase)
  name: string;                 // First name — rendered italic inside H2 via split-on-substring
  headline: string;             // Full H2 text that contains `name` as a substring
  credentials: string[];        // 2-4 bio paragraphs
  image: { src: string; alt: string };
  flip?: boolean;               // Default false. true = image LEFT, text RIGHT.
  cta: { label: string; href: string };
  specialties: Specialty[];     // Recommend 3 items
  audienceHeading: string;      // H3 above the audience list
  audienceItems: AudienceItem[];// Recommend 3-4 items
}
```

<!-- Allan's About section closing (index.astro line 152-153) — insertion point is BETWEEN this and the Companies section comment -->

```astro
        </div>  <!-- end image side -->
      </div>    <!-- end grid -->
    </SectionWrapper>
  </section>    <!-- line 153: end About section -->

  <!-- Companies Section -->     <!-- line 155: insertion point is on line 154 (between these two) -->
```

<!-- Pattern reference: existing imports at top of index.astro (lines 2-7) — new MentorIntro import slots into this block -->

```astro
import Base from "../layouts/Base.astro";
import SectionWrapper from "../components/SectionWrapper.astro";
import Button from "../components/Button.astro";
import Card from "../components/Card.astro";
import AccordionItem from "../components/AccordionItem.astro";
import FormInput from "../components/FormInput.astro";
```

<!-- Pattern reference: existing data blocks (metrics, services, results) in frontmatter — new lachlan* consts follow this pattern -->

```typescript
const services = [
  { name: "Growth Strategy", icon: "analytics", desc: "..." },
  ...
];
```

<!-- Asset confirmed on disk per Phase 6 SUMMARY: -->
<!-- public/images/lachlan/lachlan-portrait.webp — 164,094 bytes — RENDER-READY -->
<!-- Icons confirmed on disk: analytics.webp, team-training.webp, automation.webp (in public/images/icons/) -->

<!-- Calendly URL — already used 3× in codebase, locked placeholder per MENTOR-07 / STATE.md decision -->
<!-- https://calendly.com/allan-chan-roseyco/one-on-one -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MentorIntro import and three Lachlan content data blocks to index.astro frontmatter</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (frontmatter lines 1-30 — confirm exact import order and existing const patterns for `metrics`, `services`, `results`)
    - src/components/MentorIntro.astro (Props interface lines 14-38 — confirm exact prop names and types this task feeds)
    - .planning/phases/07-lachlan-section-integration-navigation-anchors/07-UI-SPEC.md (§Copywriting Contract — credentials, specialties, audience copy is LOCKED VERBATIM)
    - .planning/phases/07-lachlan-section-integration-navigation-anchors/07-RESEARCH.md (Pitfall 4 banned-words list; Pitfall 5 boltloop framing)
  </read_first>
  <action>
    Edit `src/pages/index.astro` frontmatter (lines 1-30, between the two `---` fences).

    **Sub-edit A — Add import.** Append to the existing import block (after `FormInput` on line 7) this exact line:
    ```astro
    import MentorIntro from "../components/MentorIntro.astro";
    ```
    Final import order must be: Base, SectionWrapper, Button, Card, AccordionItem, FormInput, MentorIntro.

    **Sub-edit B — Add three Lachlan data blocks.** After the existing `results` const (which ends on line 29) and BEFORE the closing `---` fence (line 30), insert these three locked-content `const` declarations VERBATIM. The strings inside MUST be byte-for-byte identical to UI-SPEC §Copywriting Contract.

    ```typescript
    const lachlanCredentials = [
      "I'm co-founder of Bellum Advisors and CEO of boltloop.co — a division of Elevateo Co focused on AI and automation for operator-owned businesses.",
      "Before that, I spent years scaling solo agencies from founder-led chaos into systemized, process-run teams. I know the exact bottlenecks because I've lived them.",
      "Outside work I train — physical discipline is non-negotiable for me, and it has shaped how I show up in business: consistent, precise, and willing to do the unglamorous reps.",
    ];

    const lachlanSpecialties = [
      { icon: "analytics",     name: "Solo Agency Scaling", desc: "From founder-led to systemized — build an agency that runs on process, not your calendar." },
      { icon: "team-training", name: "Co-Founder Operator", desc: "I partner directly with founders to run operations, sales, or delivery so you can focus on growth." },
      { icon: "automation",    name: "AI & Automation",     desc: "As CEO of boltloop.co I build custom AI systems that eliminate the repetitive work inside your business." },
    ];

    const lachlanAudience = [
      { title: "Solo agency owners",   desc: "you are billing well but can't get off the delivery treadmill" },
      { title: "Early-stage founders", desc: "you need an operator partner, not another consultant" },
      { title: "Ops-heavy businesses", desc: "your team is drowning in repetitive tasks AI should be doing" },
    ];
    ```

    **Banned-words discipline (per Pitfall 4):** Lachlan copy must NOT contain any of: `beast`, `warrior`, `grind`, `alpha`, `savage`, `crush it`, `dominate`, `kill it`. The credentials drafted above pass this check (only "discipline", "consistent", "precise", "reps" appear — all framed as work-ethic metaphor).

    **boltloop framing discipline (per Pitfall 5):** boltloop is described as "a division of Elevateo Co" — this aligns with PROJECT.md line 67 and resolves the Companies-section double-claim. Do NOT alter this phrasing.

    Do NOT modify the existing `metrics`, `services`, or `results` const blocks. Do NOT add any other imports or const blocks in this task.
  </action>
  <verify>
    <automated>grep -q 'import MentorIntro from "../components/MentorIntro.astro"' src/pages/index.astro &amp;&amp; grep -q 'const lachlanCredentials' src/pages/index.astro &amp;&amp; grep -q 'const lachlanSpecialties' src/pages/index.astro &amp;&amp; grep -q 'const lachlanAudience' src/pages/index.astro &amp;&amp; grep -q 'Bellum Advisors' src/pages/index.astro &amp;&amp; grep -q 'a division of Elevateo Co' src/pages/index.astro &amp;&amp; ! grep -iE 'beast|warrior|grind|\balpha\b|savage|crush it|dominate|kill it' src/pages/index.astro &amp;&amp; npx astro check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import MentorIntro from "../components/MentorIntro.astro"' src/pages/index.astro` returns exactly `1`
    - `grep -c 'const lachlanCredentials' src/pages/index.astro` returns exactly `1`
    - `grep -c 'const lachlanSpecialties' src/pages/index.astro` returns exactly `1`
    - `grep -c 'const lachlanAudience' src/pages/index.astro` returns exactly `1`
    - `grep -q "co-founder of Bellum Advisors and CEO of boltloop.co" src/pages/index.astro` exits 0 (verbatim credential 1)
    - `grep -q "scaling solo agencies from founder-led chaos" src/pages/index.astro` exits 0 (verbatim credential 2)
    - `grep -q "physical discipline is non-negotiable for me" src/pages/index.astro` exits 0 (verbatim credential 3 — fighting reference passes Pitfall 4)
    - `grep -q "a division of Elevateo Co" src/pages/index.astro` exits 0 (resolves boltloop double-claim per Pitfall 5)
    - `grep -q "Solo Agency Scaling" src/pages/index.astro` exits 0 (specialty 1)
    - `grep -q "Co-Founder Operator" src/pages/index.astro` exits 0 (specialty 2)
    - `grep -q "AI & Automation" src/pages/index.astro` exits 0 (specialty 3)
    - `grep -q "Solo agency owners" src/pages/index.astro` exits 0 (audience 1)
    - `grep -q "Early-stage founders" src/pages/index.astro` exits 0 (audience 2)
    - `grep -q "Ops-heavy businesses" src/pages/index.astro` exits 0 (audience 3)
    - `grep -iE 'beast|warrior|grind|\balpha\b|savage|crush it|dominate|kill it' src/pages/index.astro` exits NON-ZERO (banned-words check from Pitfall 4 passes)
    - `npx astro check` exits 0 (no TS errors — Specialty/AudienceItem shapes match MentorIntro Props)
  </acceptance_criteria>
  <done>Frontmatter contains the MentorIntro import plus three locked-copy data blocks; banned-words check passes; astro check green.</done>
</task>

<task type="auto">
  <name>Task 2: Insert hair-line divider and MentorIntro invocation between Allan's About section and the Companies section</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (lines 153-156 — confirm the exact line where Allan's `</section>` closes and where `<!-- Companies Section -->` begins)
    - src/components/MentorIntro.astro (lines 60, 64-82 — confirm `id` lands on outer section; confirm flip=true renders image-left block first)
    - .planning/phases/07-lachlan-section-integration-navigation-anchors/07-UI-SPEC.md (§Visual divider — exact divider mechanism; §Copywriting Contract — exact prop values)
    - .planning/phases/07-lachlan-section-integration-navigation-anchors/07-01-PLAN.md (Plan 01 will have added id="allan" to line 109 by the time this task runs — verify it's present)
  </read_first>
  <action>
    Edit `src/pages/index.astro` template body. Locate the closing `</section>` of Allan's About section (currently line 153 — content: `  </section>`) and the `<!-- Companies Section -->` comment (currently line 155).

    Between these two lines, insert exactly the following block. The hair-line divider goes FIRST (immediately after Allan's `</section>` close), then the MentorIntro invocation. Keep one blank line above the divider for readability.

    ```astro

      <!-- Allan → Lachlan transition divider (MENTOR-01) -->
      <div class="border-t border-white/5"></div>

      <!-- Lachlan Section (MENTOR-01..07) -->
      <MentorIntro
        id="lachlan"
        eyebrow="The Specialist"
        name="Lachlan"
        headline="Meet Lachlan"
        credentials={lachlanCredentials}
        image={{ src: "/images/lachlan/lachlan-portrait.webp", alt: "Lachlan MacDonald" }}
        flip={true}
        cta={{ label: "Book a Call", href: "https://calendly.com/allan-chan-roseyco/one-on-one" }}
        specialties={lachlanSpecialties}
        audienceHeading="Who Lachlan works with"
        audienceItems={lachlanAudience}
      />

    ```

    **Prop value notes (each value is locked by UI-SPEC §Copywriting Contract — do NOT improvise):**
    - `id="lachlan"` — lands on `MentorIntro.astro:60` outer `<section>`; resolves the `#lachlan` anchor delivered by Plan 01.
    - `eyebrow="The Specialist"` — gold uppercase label above the H2 (per UI-SPEC line 138).
    - `name="Lachlan"` and `headline="Meet Lachlan"` — MentorIntro splits headline on `name` so "Meet " renders regular and "Lachlan" renders italic light via the inherited first-name span (UI-SPEC line 139).
    - `image.src="/images/lachlan/lachlan-portrait.webp"` — confirmed on disk at 164,094 bytes from Phase 6.
    - `image.alt="Lachlan MacDonald"` — per UI-SPEC §Accessibility Contract.
    - `flip={true}` — image LEFT, text RIGHT at lg+ (resolves MENTOR-02; opposite of Allan's image-RIGHT layout).
    - `cta.href="https://calendly.com/allan-chan-roseyco/one-on-one"` — Allan's Calendly URL, intentional placeholder per MENTOR-07 / STATE.md (will swap when Lachlan's own link is provided; tracked as a STATE.md pending todo, not a Phase 7 task).
    - `cta.label="Book a Call"` — matches the header CTA wording.

    **Divider mechanism (per UI-SPEC §Visual divider):** Bare sibling `<div class="border-t border-white/5"></div>` — 1px hair-line, rgba(255,255,255,0.05), matches the existing `border-b border-white/5` on the Companies SectionWrapper (line 156). Do NOT wrap MentorIntro in a div with the border. Do NOT use a centered ornamental divider. Do NOT modify MentorIntro to accept a `topBorder` prop.

    **Do NOT:**
    - Move, rename, or modify the `<!-- Companies Section -->` comment or any subsequent section.
    - Modify Allan's About section above line 153 (Plan 01 already added `id="allan"` to line 109; leave it alone).
    - Add additional spacing utilities to the divider — MentorIntro's own `py-20 sm:py-32` provides ~320px breathing room.
    - Pass any prop NOT listed above; MentorIntro's TypeScript Props interface is strict and unrecognised props will fail `astro check`.
  </action>
  <verify>
    <automated>grep -c '<MentorIntro' src/pages/index.astro &amp;&amp; grep -q 'id="lachlan"' src/pages/index.astro &amp;&amp; grep -q 'eyebrow="The Specialist"' src/pages/index.astro &amp;&amp; grep -q 'flip={true}' src/pages/index.astro &amp;&amp; grep -q 'lachlan-portrait.webp' src/pages/index.astro &amp;&amp; grep -q 'audienceHeading="Who Lachlan works with"' src/pages/index.astro &amp;&amp; grep -B1 -A1 '<MentorIntro' src/pages/index.astro | grep -q 'border-t border-white/5' &amp;&amp; grep -c 'calendly.com/allan-chan-roseyco/one-on-one' src/pages/index.astro &amp;&amp; npx astro check &amp;&amp; npx astro build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<MentorIntro' src/pages/index.astro` returns exactly `1` (single invocation)
    - `grep -q 'id="lachlan"' src/pages/index.astro` exits 0 (resolves Plan 01's #lachlan anchor)
    - `grep -q 'eyebrow="The Specialist"' src/pages/index.astro` exits 0
    - `grep -q 'name="Lachlan"' src/pages/index.astro` exits 0
    - `grep -q 'headline="Meet Lachlan"' src/pages/index.astro` exits 0
    - `grep -q 'flip={true}' src/pages/index.astro` exits 0 (MENTOR-02: image LEFT)
    - `grep -q 'src: "/images/lachlan/lachlan-portrait.webp"' src/pages/index.astro` exits 0
    - `grep -q 'alt: "Lachlan MacDonald"' src/pages/index.astro` exits 0
    - `grep -q 'audienceHeading="Who Lachlan works with"' src/pages/index.astro` exits 0
    - `grep -q 'credentials={lachlanCredentials}' src/pages/index.astro` exits 0 (wires Task 1's data block)
    - `grep -q 'specialties={lachlanSpecialties}' src/pages/index.astro` exits 0
    - `grep -q 'audienceItems={lachlanAudience}' src/pages/index.astro` exits 0
    - `grep -q 'href: "https://calendly.com/allan-chan-roseyco/one-on-one"' src/pages/index.astro` exits 0 (MENTOR-07 placeholder)
    - `grep -c 'calendly.com/allan-chan-roseyco/one-on-one' src/pages/index.astro` returns at least `3` (hero line 72, why-section line 323, new MentorIntro CTA — confirms placeholder is wired)
    - `grep -q 'border-t border-white/5' src/pages/index.astro` returns multiple matches (the new divider + the existing Companies section's `border-b`-adjacent uses) — at minimum the divider must exist immediately preceding the MentorIntro invocation
    - `grep -q 'Allan → Lachlan transition divider (MENTOR-01)' src/pages/index.astro` exits 0 (anchor comment for the divider — discoverable for future maintainers)
    - `grep -q 'Lachlan Section (MENTOR-01..07)' src/pages/index.astro` exits 0 (anchor comment for the section)
    - `grep -q '<!-- Companies Section -->' src/pages/index.astro` exits 0 (downstream comment preserved)
    - `npx astro check` exits 0
    - `npx astro build` exits 0 (full production build green; no broken prop types, no missing assets)
  </acceptance_criteria>
  <done>`<MentorIntro>` is invoked once between Allan's About `</section>` and the Companies section, preceded by a 1px hair-line divider; all 11 props are passed correctly; Astro build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Lachlan section visual, copy tone, anchor scroll, CTA routing</name>
  <files>(no edits — pure visual + tone verification)</files>
  <read_first>
    - .planning/phases/07-lachlan-section-integration-navigation-anchors/07-UI-SPEC.md (§Copywriting Contract banned-words list, §Visual divider, §Responsive Behavior)
    - .planning/phases/07-lachlan-section-integration-navigation-anchors/07-VALIDATION.md (§Manual-Only Verifications)
    - .planning/phases/07-lachlan-section-integration-navigation-anchors/07-RESEARCH.md (Pitfall 1 — Lenis offset sign verification; Pitfall 4 — fighting tone; Pitfall 5 — boltloop framing)
  </read_first>
  <what-built>
    Plan 01 wired Lenis anchor support + 3 header anchor links + `id="allan"` / `id="faq"`.
    Plan 02 Tasks 1–2 inserted Lachlan's full mentor section (bio split-grid + 3 specialty cards + audience list + CTA) into index.astro between Allan's About section and the Companies section, with a hair-line divider above and `id="lachlan"` for the header's `#lachlan` anchor.
  </what-built>
  <how-to-verify>
    Executor runs `pnpm dev` and opens `http://localhost:4321/`. Run through this checklist in order:

    **A. Anchor scroll — Lenis offset sign verification (Pitfall 1):**
    1. Click header link `Lachlan`. Confirm: page smooth-scrolls down; the gold "The Specialist" eyebrow label is fully visible just below the 72px fixed header (not hidden under it).
    2. Click header link `Allan`. Confirm: page smooth-scrolls up; the gold "The Mentor" eyebrow label on Allan's section is fully visible.
    3. Click header link `FAQ`. Confirm: page smooth-scrolls; the "Clarity" eyebrow + "Questions People Ask Me" heading visible below header.
    4. URL bar updates to `#lachlan`, `#allan`, `#faq` respectively after each click.
    5. **If any target sits hidden under the header:** flip `offset: -72` to `offset: 72` in `src/layouts/Base.astro`, update the code comment, re-test, then re-commit.

    **B. Visual layout (MENTOR-01, MENTOR-02):**
    6. Scroll past Allan's image. Confirm a thin hair-line is visible at the section boundary (rgba(255,255,255,0.05)).
    7. At desktop width (≥ 1024px), confirm Lachlan's photo is on the LEFT and bio text is on the RIGHT (flipped vs. Allan's image-on-the-right layout above).
    8. At mobile width (375px), confirm photo stacks ABOVE text (single column).

    **C. Copy tone (MENTOR-03, MENTOR-04, MENTOR-05, MENTOR-06):**
    9. Read all three credential paragraphs. Confirm:
       - Paragraph 1 names "Bellum Advisors" and "boltloop.co" framed as "a division of Elevateo Co" (NOT "I founded boltloop", NOT "I own boltloop") — passes Pitfall 5.
       - Paragraph 2 mentions scaling solo agencies / founder-led chaos / process-run teams.
       - Paragraph 3 contains the discipline reference. Confirm it uses words like "discipline / non-negotiable / consistent / precise / reps". Confirm it does NOT contain ANY of: `beast`, `warrior`, `grind`, `alpha`, `savage`, `crush it`, `dominate`, `kill it`.
    10. Confirm three specialty cards render with: `Solo Agency Scaling` (analytics icon), `Co-Founder Operator` (team-training icon), `AI & Automation` (automation icon).
    11. Confirm "Who Lachlan works with" block renders 3 audience items: `Solo agency owners — you are billing well…`, `Early-stage founders — you need an operator partner…`, `Ops-heavy businesses — your team is drowning…`.

    **D. CTA routing (MENTOR-07):**
    12. Click the `Book a Call` button under Lachlan's bio. Confirm: opens `https://calendly.com/allan-chan-roseyco/one-on-one` in a NEW tab (target="_blank" + rel="noopener noreferrer" inherited from MentorIntro `cta` → Button.astro).

    **E. Responsive sanity (NAV-04 mobile):**
    13. Open dev tools → 375px viewport. Open hamburger menu. Confirm 3 links (Allan / Lachlan / FAQ) plus the Book a Call CTA button. Tap each anchor → menu auto-closes and page smooth-scrolls.

    **F. Build hygiene:**
    14. Run `npx astro build` once more — confirm it exits 0 with no warnings about missing assets, broken imports, or template errors.

    **G. Image quality (sanity check):**
    15. Confirm `lachlan-portrait.webp` renders without distortion — face proportions correct (no horizontal squish from EXIF orientation issue, which Phase 6 already fixed).
  </how-to-verify>
  <action>Pause execution. Present the verification checklist (the <how-to-verify> block above) to the human user. The user runs through all 15 checks in their browser against `pnpm dev` on `http://localhost:4321/`. Block on the user typing `approved` per <resume-signal>. If any check fails, the user describes the failure; the executor applies the documented fix (e.g. flip Lenis offset sign per Pitfall 1), re-runs the affected check, then waits for re-approval. Do NOT auto-approve. Do NOT proceed to commit until the user explicitly approves.</action>
  <verify>
    <automated>echo "Manual verification gate — see <how-to-verify> for the 15 checks the human runs in the browser. No automated grep can substitute for the visual + tone confirmation in this task."</automated>
  </verify>
  <done>Human user has typed `approved` after running all 15 checks; any Pitfall-1 sign-flip on Lenis offset is committed; `npx astro build` re-confirmed green from inside the gate.</done>
  <resume-signal>Type `approved` once all 15 checks pass. If any check fails, describe the failure (e.g. "Pitfall 1: target hidden under header — flipped to offset:72 and re-verified") and re-run the affected check before approving.</resume-signal>
</task>

</tasks>

<verification>
After all 3 tasks (including the human checkpoint):

1. `npx astro check` exits 0.
2. `npx astro build` exits 0.
3. `grep -c 'href="#allan"\|href="#lachlan"\|href="#faq"'` against the BUILT html output (`dist/index.html`) shows each anchor at least twice (once in desktop nav, once in mobile menu).
4. The full Lachlan section is rendered in `dist/index.html` — search for "The Specialist", "Bellum Advisors", "Who Lachlan works with" — all present.
5. Lenis offset sign is empirically verified and documented in the Base.astro code comment.
6. Tone of fighting reference passes the Pitfall 4 banned-words check (also enforced by Task 1's automated grep, but human re-confirms tone in Task 3).
</verification>

<success_criteria>
- All MENTOR-01 through MENTOR-07 requirements satisfied (visual break + flipped split-grid + credentials + tasteful discipline reference + 3 specialty cards + audience block + CTA to Allan's Calendly).
- NAV-04 fully resolved (combined with Plan 01): 3 header anchor links, all three resolve to visible targets below the 72px header on desktop and mobile.
- Production build green; no broken assets, no missing icons, no TS errors.
- Lachlan section is the most copy-sensitive deliverable in the milestone — the human checkpoint guarantees tone passes before commit.
- Known carry-forward (NOT blocking Phase 7): Lachlan's own Calendly URL still pending — STATE.md tracks as a future patch swap, not a Phase 7 task.
</success_criteria>

<output>
After completion, create `.planning/phases/07-lachlan-section-integration-navigation-anchors/07-02-SUMMARY.md` documenting:
- Final Lenis offset value (-72 or 72 — whichever passed empirical test in Task 3 step 1)
- Confirmation that all 7 MENTOR requirements + NAV-04 are now resolved end-to-end
- Note: Lachlan's own Calendly URL still pending (placeholder routes to Allan's link per MENTOR-07; STATE.md pending todo)
- Note: `lachlan-warroom.webp` (149.8 KB) remains unused on disk per RESEARCH Open Question 4 — not deleted, not used; left for potential future "behind the scenes" use case
- Build size delta vs Phase 6 baseline (informational)
</output>
