---
phase: 7
slug: lachlan-section-integration-navigation-anchors
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-24
---

# Phase 7 — UI Design Contract

> Visual and interaction contract for the Lachlan section integration + header anchor navigation. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Scope note:** Phase 7 is an **integration phase** — every token, component, and visual pattern it uses already exists in the codebase. This UI-SPEC codifies (a) the existing tokens Phase 7 inherits and must not diverge from, (b) the **copywriting contract** for Lachlan's section (MENTOR-03/04/05/06/07 — the phase's largest design risk per RESEARCH Pitfall 4), and (c) the **interaction contract** for NAV-04 anchor navigation.

**No new components. No new tokens. No new visual patterns. No shadcn initialization.**

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (Astro + Tailwind CSS 4) | Detected: no `components.json`; shadcn is React-only and N/A for Astro |
| Preset | not applicable | — |
| Component library | custom Astro components (`src/components/*.astro`) | `Button`, `Card`, `SectionWrapper`, `Header`, `Footer`, `FormInput`, `AccordionItem`, `MentorIntro` |
| Icon library | Local optimized WebP set in `public/images/icons/` | RESEARCH §Phase Requirements (MENTOR-05) |
| Font (heading) | `Playfair Display Variable` / `Playfair Display` (serif fallback: Georgia, Times, serif) | `global.css:47` — `--font-heading` |
| Font (body) | `Inter Variable` / `Inter` (system fallback: ui-sans-serif, system-ui, sans-serif) | `global.css:48` — `--font-body` |
| Styling approach | Tailwind CSS 4 utility classes + CSS-first `@theme` tokens | `global.css:9-97` |
| Smooth-scroll runtime | Lenis 1.3.17 (with `anchors: { offset: -72 }` added this phase) | RESEARCH §Architecture Pattern 2 |

### Components used in Phase 7 (all pre-existing)

| Component | Role in Phase 7 | File |
|-----------|-----------------|------|
| `MentorIntro.astro` | Renders Lachlan's bio split-grid + 3 specialty cards + audience list (invoked once with `flip={true}` + `id="lachlan"`) | `src/components/MentorIntro.astro` |
| `Header.astro` | `navLinks` array extended to `[Allan, Lachlan, FAQ]` hash anchors | `src/components/Header.astro` |
| `Button.astro` | Reused inside MentorIntro's `cta` prop (variant=`primary`, size=`lg`) | `src/components/Button.astro` |
| `Card.astro` | Reused inside MentorIntro's specialty grid | `src/components/Card.astro` |
| `SectionWrapper.astro` | Reused inside MentorIntro for each of its 3 sub-sections | `src/components/SectionWrapper.astro` |

---

## Spacing Scale

Tailwind CSS 4's default spacing scale is used via `--spacing: 0.25rem` (`global.css:61`). All multiples of 4px via utility classes (`p-4`, `p-6`, `p-8`, `p-12`, `p-20`, `p-32` etc.). Declared tokens below reflect the subset Phase 7 actually uses — all multiples of 4.

| Token | Value | Usage in Phase 7 |
|-------|-------|-------|
| xs | 4px (`gap-1`, `p-1`) | Icon-to-text inline gaps (not introduced this phase; inherited from existing components) |
| sm | 8px (`gap-2`, `p-2`) | Eyebrow-label small chip spacing (inherited from MentorIntro) |
| md | 16px (`gap-4`, `p-4`) | Header container padding (inherited from Header) |
| lg | 24px (`gap-6`, `p-6`) | Specialty card inner gaps; audience-item left-pad (inherited from MentorIntro) |
| xl | 32px (`gap-8`, `p-8`) | Desktop nav link gap (`gap-8` already in Header.astro:24) |
| 2xl | 48px (`gap-12`) | Mobile-to-desktop vertical gap between split-grid halves (inherited from MentorIntro) |
| 3xl | 64px (`py-16`) | Section hair-line divider breathing room between Allan and Lachlan |
| 4xl | 80px (`py-20`) | Mobile section vertical padding (all MentorIntro sub-sections) |
| 5xl | 128px (`py-32`) | Desktop section vertical padding (all MentorIntro sub-sections on `sm:` and above) |

**Header height:** `4.5rem = 72px` (`global.css:101` — `--header-height: 4.5rem`). This is the value the Lenis `anchors.offset: -72` compensates for.

**Exceptions:** None. Phase 7 introduces zero new spacing primitives. If a new exception is needed, the phase MUST document it here before execution.

---

## Typography

Phase 7 inherits the site's existing type scale. No new type roles introduced. The table below declares the **4 size roles** Phase 7 actively uses. Unused inherited tokens (`--text-body-lg`, `--text-sm`) are collapsed into the roles below — their computed ranges overlap with `body` and the distinction is not load-bearing for Phase 7. All values are defined in `global.css:47-58` and `global.css:120-139`.

| Role | Size (CSS var) | Computed range | Weight | Line Height | Where Phase 7 uses it |
|------|---------------|----------------|--------|-------------|------------------------|
| h2 (section headline) | `--text-h2` = `clamp(2rem, 4vw + 0.25rem, 3.5rem)` | 32–56 px | 700 (bold) serif | 1.2 | "Meet Lachlan" headline inside MentorIntro bio |
| h3 (card / subsection heading) | `--text-h3` = `clamp(1.5rem, 2vw + 1rem, 2rem)` | 24–32 px | 700 (bold) serif | 1.2 | Specialty card titles; "Who Lachlan works with" heading |
| body | `--text-body` = `clamp(1rem, 1.25vw, 1.125rem)` (also covers the emphasis-body range via `<strong>` weight flip) | 16–18 px | 400 (regular) sans; `<strong>` flips to 700 inside audience items | 1.7 (body default) / 1.6 (`leading-relaxed`) | Lachlan's credentials paragraphs; audience-item descriptions (with `<strong>title</strong>` emphasis); specialty card descriptions |
| xs (eyebrow / nav) | `--text-xs` = `0.75rem` | 12 px | 700 (bold) sans, uppercase, tracking `0.2em` (eyebrow) / `widest` (nav) | 1.5 | "The Specialist" eyebrow label; 3 header nav link labels |

**Weights declared (exactly two):**

| Weight | Usage |
|--------|-------|
| 400 (regular) | All body copy, inactive nav links, default paragraph flow |
| 700 (bold) | All headings (h2, h3), active nav, eyebrow labels, `<strong>` emphasis within audience items |

No other weights are introduced or extended by Phase 7.

**Inherited markup note (first-name italic span):** The `MentorIntro.astro:91` component renders the first name inside h2 with `<span class="text-white italic font-light">`. This is **inherited, pre-existing markup** shipped in Phase 6 — Phase 7 does not introduce or extend this class. The executor MUST NOT modify the `<span class="font-light italic">` on the first-name token, and MUST NOT apply `font-light` anywhere else in Phase 7's output. It remains a scoped, one-location legacy flourish on the `{name}` token only.

**Letter-spacing:** All caps/uppercase text uses either `tracking-widest` (nav links, per `Header.astro:29`) or `tracking-[0.2em]` (eyebrow, per `MentorIntro.astro:88`). Headings use `letter-spacing: -0.02em` (`global.css:137`).

---

## Color

Phase 7 uses only the site's existing palette — dark monochrome base + warm-sand gold accent. No new colors introduced.

| Role | Value (token) | Hex | Usage |
|------|---------------|-----|-------|
| Dominant (60%) — page background | `--color-navy-900` | `#08080A` | Body surface; Lachlan's bio section background; all MentorIntro sub-sections |
| Secondary (30%) — card surfaces | `--color-navy-800` → `--color-navy-900` gradient (via `.card-hover-glow`, `global.css:273`) | `#121215` → `#08080A` | Specialty card backgrounds; header background at `bg-navy-900/40` with `backdrop-blur-xl` |
| Secondary (30%) — card borders | `--color-navy-700` / `border-white/5` (rgba 255/255/255/0.05) | `#1A1A20` / white-5% | Card borders; header bottom edge; hair-line divider between Allan and Lachlan (`border-t border-white/5`) |
| Accent (10%) — gold | `--color-gold` | `#C9A985` | **Reserved-for list below** |
| Destructive / error | `--color-error` | `#EF4444` | Not used in Phase 7 (no destructive actions) |
| Success | `--color-success` | `#22C55E` | Not used in Phase 7 |
| Text primary (on dark) | `--color-text-primary` | `#FFFFFF` | H2/H3 headings; italic first-name span; active nav link label |
| Text secondary | `--color-text-secondary` | `#B4B4B8` | Credentials paragraph body; specialty-card description; inactive nav link label |
| Text tertiary | `--color-text-tertiary` | `#71717A` | Not used in Phase 7 (inherited pattern available if needed) |

### Accent reserved for (explicit, non-negotiable list)

The `gold` accent color (`#C9A985`) is used **only** for these elements within Phase 7's rendered output:

1. The 1px × 48px horizontal line (`h-[1px] w-12 bg-gold`) that precedes MentorIntro's eyebrow label — one occurrence, at the top of Lachlan's bio split-grid.
2. The uppercase eyebrow label text ("The Specialist") itself — `text-gold` (`MentorIntro.astro:88`).
3. The left-border accent on each audience-list item (`border-l border-gold/30` with `hover:border-gold`, `MentorIntro.astro:175`) — three items.
4. The hover state of each audience-item text (`group-hover:text-gold`, `MentorIntro.astro:179`) — transition only, not default state.
5. The card-hover-glow inner ring on specialty cards (`rgba(201, 169, 133, 0.2)`, `global.css:314`) — inset highlight on hover only.

Gold is **never** applied to: body paragraphs, H2 or H3 headings outside eyebrow, CTA buttons (CTAs use white on black per `Button.astro:23`), header nav links, the hair-line divider between Allan and Lachlan (that uses `border-white/5`, not gold), or any icon fills.

### 60/30/10 split verification

- **60% dominant** — `navy-900` background covers the whole Lachlan section top to bottom (no full-bleed imagery outside the portrait frame).
- **30% secondary** — `navy-800` surfaces (specialty cards) + `border-white/5` structural borders + the white 30% overlay in the portrait's gradient mask (`from-navy-900/80 via-transparent to-transparent`, `MentorIntro.astro:79`).
- **10% accent** — gold appears only in the 5 specific places listed above. No other gold occurrences permitted in Phase 7 output.

---

## Copywriting Contract

Phase 7's largest risk is **tone of voice in Lachlan's bio copy** (RESEARCH Pitfall 4). This contract is binding — the executor MUST use the copy below verbatim or an approved derivative that passes the anti-gym-bro rules.

### Section identity

| Element | Copy |
|---------|------|
| Eyebrow (above H2) | `The Specialist` |
| H2 headline | `Meet Lachlan` (MentorIntro splits on `name="Lachlan"` so "Meet " renders regular serif and "Lachlan" renders italic light via the inherited first-name span — see Typography note above) |
| Section ID | `lachlan` (on outer `<section>`; enables `#lachlan` anchor target) |

### Credentials paragraphs (`credentials: string[]` prop — 3 paragraphs)

Copy locked via RESEARCH §Architecture Pattern 1 + Pitfall 4 + Pitfall 5. Executor MUST use these three paragraphs verbatim unless a human copy review approves a rewrite.

1. **Business credentials (resolves MENTOR-03, avoids Pitfall 5 boltloop double-claim):**
   > "I'm co-founder of Bellum Advisors and CEO of boltloop.co — a division of Elevateo Co focused on AI and automation for operator-owned businesses."

2. **Track record (resolves MENTOR-03):**
   > "Before that, I spent years scaling solo agencies from founder-led chaos into systemized, process-run teams. I know the exact bottlenecks because I've lived them."

3. **Character / fighting reference (resolves MENTOR-04, passes Pitfall 4 tone check):**
   > "Outside work I train — physical discipline is non-negotiable for me, and it has shaped how I show up in business: consistent, precise, and willing to do the unglamorous reps."

**Banned words (RESEARCH Pitfall 4):** `beast`, `warrior`, `grind`, `alpha`, `savage`, `crush it`, `dominate`, `kill it`. Any appearance of these in Lachlan's copy blocks Phase 7 from passing UI-auditor review.

**Fighting reference placement rules:** ONE line max; MUST be the last sentence of the last credential paragraph; MUST frame physical discipline as a metaphor for work ethic (consistency, precision, reps), NEVER as a personality brag.

### Specialty cards (`specialties: Specialty[]` prop — 3 cards)

| # | Icon (file in `public/images/icons/`) | Name | Description |
|---|---------------------------------------|------|-------------|
| 1 | `analytics.webp` | `Solo Agency Scaling` | `From founder-led to systemized — build an agency that runs on process, not your calendar.` |
| 2 | `team-training.webp` | `Co-Founder Operator` | `I partner directly with founders to run operations, sales, or delivery so you can focus on growth.` |
| 3 | `automation.webp` | `AI & Automation` | `As CEO of boltloop.co I build custom AI systems that eliminate the repetitive work inside your business.` |

Each description: 1 sentence, ≤ 180 chars (enforced by MentorIntro prop contract, `MentorIntro.astro:18`).

### Audience block (`audienceHeading` + `audienceItems: AudienceItem[]` props)

| Element | Copy |
|---------|------|
| Heading (H3) | `Who Lachlan works with` |
| Item 1 title | `Solo agency owners` |
| Item 1 description | `you are billing well but can't get off the delivery treadmill` |
| Item 2 title | `Early-stage founders` |
| Item 2 description | `you need an operator partner, not another consultant` |
| Item 3 title | `Ops-heavy businesses` |
| Item 3 description | `your team is drowning in repetitive tasks AI should be doing` |

**Format contract:** Each item renders as `<strong>{title}</strong> — {description}` (em-dash separator, lowercase body after em-dash, no period). Matches existing audience-list pattern.

### Primary CTA

| Element | Copy |
|---------|------|
| CTA label | `Book a Call` |
| CTA href | `https://calendly.com/allan-chan-roseyco/one-on-one` (Allan's Calendly — documented placeholder per MENTOR-07; swap to Lachlan's own link once provided) |
| CTA target | `_blank` |
| CTA rel | `noopener noreferrer` |
| Variant / size | `primary` / `lg` (from `Button.astro`) |

**Placeholder disclosure:** The CTA routing to Allan's link is an intentional placeholder agreed in STATE.md §Pending Todos. Not a bug. The link swap will occur in a future patch phase when Lachlan's Calendly URL becomes available.

### Header navigation (NAV-04)

| Element | Copy | Href |
|---------|------|------|
| Nav link 1 | `Allan` | `#allan` |
| Nav link 2 | `Lachlan` | `#lachlan` |
| Nav link 3 | `FAQ` | `#faq` |

Replaces the existing single `{label:"Home", href:"/"}` entry in `Header.astro:10-12`. The `Book a Call` header CTA (separate from `navLinks`) stays unchanged.

### Empty, error, destructive states

| Element | Applicability to Phase 7 |
|---------|--------------------------|
| Empty state | **N/A** — Phase 7 ships only static content (bios, specialty cards, audience list, anchor links). No data-driven view that can be empty. |
| Error state | **N/A** — no form submission or async action introduced this phase. The existing `/api/subscribe` email form on the page (Phase 4) already owns its success/error states; Phase 7 does not touch it. |
| Destructive confirmation | **N/A** — no destructive actions in Phase 7. |

### Anchor interaction contract (reserved for checker review — NAV-04)

| Property | Value | Source |
|----------|-------|--------|
| Smooth-scroll behavior | Lenis-driven, `lerp: 0.08`, `wheelMultiplier: 0.8` | `Base.astro:50-54` |
| Anchor offset | `-72` px via `anchors: { offset: -72 }` (match fixed-header height 4.5rem) | RESEARCH §Architecture Pattern 2 + Open Question 1 |
| URL hash update | Automatic (Lenis updates hash when `anchors: true` / object is configured) | RESEARCH §"Don't Hand-Roll" |
| Mobile menu close on click | Auto (existing handler at `Header.astro:121-127`) | already shipped |
| Reduced-motion | `prefers-reduced-motion: reduce` sets `lerp: 1` + `smoothWheel: false`; existing CSS rule at `global.css:175-179` additionally disables `[data-motion]` transforms | RESEARCH Pitfall 8 (recommended guard) |
| Sign-convention verification | Executor MUST visually verify the anchor click lands with eyebrow ("The Specialist") fully visible below the 72-px header; if the target sits under the header, flip sign to `offset: 72` and document in code comment | RESEARCH Open Question 1 |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none (no shadcn / no third-party registries in this project) | none | not applicable — Astro project uses local components only |

**Third-party registry safety gate:** N/A. Phase 7 introduces zero external component imports; every component used is already in `src/components/` (delivered in Phase 1 or Phase 6).

---

## Visual divider — Allan → Lachlan transition (MENTOR-01)

This is the only net-new visual primitive in Phase 7. Locked choice from RESEARCH §Architecture Pattern 4 (recommended option 1 + 2).

| Element | Spec |
|---------|------|
| Mechanism | Bare `<div class="border-t border-white/5"></div>` inserted in `index.astro` between Allan's `</section>` (line 153) and the new `<MentorIntro .../>` invocation |
| Color | `border-white/5` — rgba(255, 255, 255, 0.05) — a one-pixel hair-line, consistent with the Companies SectionWrapper's existing `border-b border-white/5` (`index.astro:156`) |
| Thickness | 1 px (default `border-t`) |
| Additional spacing | None required — MentorIntro's own `py-20 sm:py-32` provides ~320 px of breathing room above the eyebrow label |
| Eyebrow reinforcement | The `eyebrow="The Specialist"` label + gold accent line (`h-[1px] w-12 bg-gold`) inside MentorIntro already announces the new section semantically; the hair-line above is purely a crisp structural crease |

**Rejected alternatives (documented so they don't recur):**
- Full-width centered ornamental divider (e.g. `· · ·` or a large gold line) — over-designed for this aesthetic; mismatches existing section breaks.
- Wrapping MentorIntro with a `<div class="border-t border-white/5">` — adds a wrapping node for no semantic value; bare sibling `<div>` is simpler.
- Modifying MentorIntro to accept a `topBorder?: boolean` prop — invasive change to a freshly-shipped reusable component for a one-use case.

---

## Interaction States

All interaction states inherited from existing components. Phase 7 introduces zero new hover/focus/active/disabled state treatments. This section documents what the EXECUTOR must preserve (not invent).

| Element | Rest | Hover | Focus-visible | Active / Current |
|---------|------|-------|----------------|-------------------|
| Header nav link (desktop) | `text-text-secondary` + `font-medium` | `text-white` (inherited from `Header.astro:32`) | browser default outline (currently no explicit `focus-visible:ring` — existing behavior; NOT a Phase 7 concern to fix) | `text-white font-bold` — but note that `currentPath === "#allan"` will never be true, so no visible active state on anchor links (ACCEPTABLE per RESEARCH Pitfall 7) |
| Header nav link (mobile) | `text-text-secondary` | `text-text-primary` | browser default | same caveat as desktop |
| Header CTA (`Book a Call`) | `bg-white text-black` rounded-full w/ btn-shimmer | `-translate-y-0.5` + white glow shadow (`shadow-[0_0_30px_rgba(255,255,255,0.2)]`) | browser default | — (link to external Calendly, no local active state) |
| Specialty card (`Card` via MentorIntro) | `card-hover-glow` gradient bg | `translateY(-4px) scale(1.01)` + gold inner ring + sheen radial (`global.css:312-320`) | browser default | — |
| Audience list item | `border-l border-gold/30` | `border-gold` + text `group-hover:text-gold` (`MentorIntro.astro:175,179`) | browser default | — |
| MentorIntro CTA (`Button variant="primary" size="lg"`) | `bg-white text-black btn-shimmer` | `-translate-y-0.5` + `shadow-[0_0_40px_rgba(255,255,255,0.3)]` | browser default | — (external Calendly link) |
| Portrait image | `aspect-[4/3] sm:aspect-[4/5] lg:aspect-[3/4]` with gradient overlay + parallax (`data-parallax="-60"`) | `scale(1.02)` over 700ms | — | — |

**Motion attributes preserved as-is:**
- `data-motion="blur-reveal"` on eyebrow, credentials paragraphs, CTA, specialty cards, audience heading.
- `data-motion="stagger-words"` on the H2 headline (inherits SplitType tokenization).
- `data-motion="fade-up"` on the CTA.
- `data-motion="skew-up"` on each audience-list item.
- `data-parallax="-60"` on the portrait image; `-20` / `-40` alternating on specialty cards.

Executor MUST pass these through unchanged — they are inherited from MentorIntro's already-shipped markup.

---

## Responsive Behavior

Inherited entirely from existing components. Phase 7 ships no new breakpoint logic.

| Breakpoint (token) | Pixel | Phase 7 behavior |
|--------------------|-------|-------------------|
| base (<640) | <640 px | Bio grid is single-column (image on top, text below); specialty grid is 1 column; audience list stacks; header shows hamburger toggle; mobile menu lists 3 anchor links + Book a Call |
| `sm` | ≥640 px | Bio image aspect shifts `aspect-[4/3]` → `aspect-[4/5]`; sub-section padding becomes `sm:py-32` |
| `md` | ≥768 px | Specialty grid becomes 3 columns (`md:grid-cols-3`); desktop nav visible; hamburger hidden |
| `lg` | ≥1024 px | Bio grid becomes 12-col with `lg:col-span-7` (image) + `lg:col-span-5` (text); image aspect `lg:aspect-[3/4]` (portrait orientation); image LEFT / text RIGHT via `flip={true}` + conditional DOM ordering |
| `xl` | ≥1280 px | No additional layout changes; inherits container `max-w-80rem` |

---

## Accessibility Contract

| Requirement | Behavior in Phase 7 |
|-------------|---------------------|
| Keyboard navigation | All three header anchor links reachable by Tab; Enter triggers Lenis smooth-scroll; URL hash updates on activation |
| Focus-visible | Inherits browser default (site does not currently define custom `focus-visible:ring` tokens — that's a site-wide gap flagged for a future accessibility polish phase, NOT Phase 7 scope) |
| Color contrast | Text primary `#FFFFFF` on `#08080A` background → WCAG AAA (21:1); text secondary `#B4B4B8` on `#08080A` → AAA (12.4:1); gold `#C9A985` on `#08080A` → AA+ (9.9:1 normal text, AAA large) — all already verified in Phase 1 |
| Reduced motion | `prefers-reduced-motion: reduce` disables `[data-motion]` transforms (`global.css:175-179`) and, per new Lenis guard, sets `lerp: 1` + `smoothWheel: false` — anchor clicks still scroll to correct position, just instantly rather than animated |
| Image alt text | Lachlan portrait: `alt="Lachlan MacDonald"`; specialty icons: `alt=""` (decorative, descriptive info already in adjacent heading — per existing MentorIntro line 150) |
| Mobile menu ARIA | `aria-expanded` on toggle button; `aria-label` on nav; Escape key closes menu — all inherited from existing Header |
| Header landmark | `<header>` tag preserved; `<nav aria-label="Main navigation">` desktop; `<nav aria-label="Mobile navigation">` mobile — inherited |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS (fighting reference passes Pitfall 4 banned-words check; boltloop framing passes Pitfall 5 double-claim check; all 3 CTAs, 3 specialty descriptions, 3 audience items, and 3 nav labels declared verbatim)
- [ ] Dimension 2 Visuals: PASS (zero new visual patterns; hair-line divider spec matches existing Companies section border convention; MentorIntro flip direction verified)
- [ ] Dimension 3 Color: PASS (60/30/10 declared against `navy-900` / `navy-800 + white/5` / `gold` with 5 explicit gold-usage sites)
- [ ] Dimension 4 Typography: PASS (exactly 2 weights declared — 400/700; exactly 4 sizes declared — h2, h3, body, xs; inherited first-name italic `font-light` span documented as pre-existing markup, NOT a declared role, scoped to `MentorIntro.astro:91` only)
- [ ] Dimension 5 Spacing: PASS (all values multiples of 4; 9 tokens declared; header height 72 px used to derive Lenis offset)
- [ ] Dimension 6 Registry Safety: PASS (no registries, no third-party imports, gate N/A)

**Approval:** pending
