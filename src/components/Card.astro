---
// src/components/Card.astro

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={[
  "card-hover-glow rounded-xl sm:rounded-2xl p-8 sm:p-10",
  className
]}>
  <slot />
</div>
